# 幽灵棋盘技术规划

## 概述

中国象棋自动对弈棋盘，当前默认使用直接拖动的扁平棋片（引磁片上贴棋子贴纸）在棋盘表面滑动。

## 当前方案

- 棋盘顶板采用 `2mm` 单层透明板，棋盘层贴在上表面
- 当前不采用永磁体锁定，不再继续更换更强的同类吸盘式电磁铁
- 电磁铁最低点不再增加额外厚度，优先直接压缩磁距
- 当前控制方式采用“按格执行”的单步基础动作，不走整条路径持续吸附拖动
- 旧方案和已放弃路线统一归档到 [`docs/archive/abandoned-options.md`](archive/abandoned-options.md)

## 系统结构

```
USB 摄像头（UVC，俯拍）
        │
        ▼
Intel NUC7i5BNH（Linux / Ubuntu）
  ├─ 外部视觉仓库 `GhostVision`
  ├─ 象棋 AI
  └─ USB 串口 → G-code
        │
        ▼
ESP32 GRBL + TMC2209 ×2
  ├─ XY 运动
  └─ PWM → MOSFET → 电磁铁
```

## 机械与棋盘

- 顶板：`2mm` 透明亚克力或 PC
- 棋盘图案：当前使用备用棋盘层贴于上表面
- 棋面载体：直接使用 `Φ20×0.3mm` 引磁片作为被拖对象，上表面贴棋子图案贴纸
- 木质棋子：已退出当前默认工作形态，仅保留早期验证记录
- 电磁铁：当前已验证 `5W` / `8W` 吸盘式型号；两者都能驱动，但都不是最终长期方向
- 木板固定：当前做法为在桐木板上打通孔后，使用 `4` 颗带垫圈的螺钉穿过木板，并以约 `8cm` 的间距拧入铝型材槽内的 `T` 型螺母，从而把木板固定到 XY 框架
- 侧面按钮优先，避免上表面入镜和误触

## 运动与控制

### 电磁铁驱动

- GRBL 激光接口输出的是约 `0V/5V` PWM 逻辑电平
- 电磁铁通过外接 MOSFET 低边开关驱动，不直接挂在板载接口上
- 指令约定：
  - `M3 Sxxx`：电磁铁通电
  - `M5`：电磁铁断电
  - `$30`：PWM 上限，当前按 `1000` 使用

### 坐标与格距

- `y` 轴平行于楚河汉界
- `x` 轴垂直于楚河汉界
- 当前控制格距基于实测有效格区：
  - `x = 370 / 9 ≈ 41.11mm/格`
  - `y = 337 / 8 = 42.125mm/格`
- 说明：
  - `370mm` 对应主棋盘沿 `x` 方向的 `9` 格总长
  - `337mm` 对应主棋盘沿 `y` 方向的 `8` 格总长
  - 以上仅统计格子区域，不含棋盘纸外圈边距

### 单步动作

- 当前默认单步动作：
  - 空载前导 `pre`
  - `M3 Sxxx` 吸合
  - 带棋移动主段
  - 低 `PWM` 过冲
  - `M5` 断电
  - 电磁铁空载回正 `overshoot`
- 当前默认初值：
  - `overshoot = 15mm`
  - `pre = 15mm`
  - `overshoot_pwm = 500`
- 当前默认速度：
  - 带棋拖动 `move_feed = 2400 mm/min`
  - 空载回正 `return_feed = 24000 mm/min`
- 当前方向是“逐格执行”，而不是整条路径持续吸着拖

### 路径规划

- 路径节点按棋盘点位中心处理
- 仅在空点通行
- 采用 4 邻接 BFS
- 普通走子按 BFS 路径逐格执行上述单步动作
- 吃子流程当前按“先移走被吃子，再移动进攻方”执行；被吃子进入右侧吃子区槽位
- 当前阶段判断：高密度区域的稳定性问题不能靠全局减小 `overshoot` 解决，否则会把当前已校准的落点重新拉偏；后续应考虑按局部邻近占位切换执行策略，或在规划层引入“远离障碍”的安全裕量

## 棋盘与 XY 平台尺寸

- 平台：雕刻机框架，工作面积 `440mm × 505mm`
- 主棋盘：`9 × 10`
- 吃子区：右侧 `2 × 10`
- 点阵总数：`11 × 10`
- 棋盘纸店家标称边长约 `40cm`，但控制参数以实测值为准
- 主棋盘实测有效格区：
  - `x` 方向：`370mm`（`9` 格）
  - `y` 方向：`337mm`（`8` 格）
- 棋盘纸外形尺寸比有效格区更大，需额外预留外圈边距

### 雕刻机 XY 框架套件包含

1. 铝型材
- 2040 型材（592mm）×2
- 2040 型材（628mm）×1
- 2020 型材（620mm）×2

2. 运动与传动组件
- T8 丝杆（600mm）×1
- Y 轴支架套件 ×1
- X 轴滑车套件 ×1
- 42 步进电机 ×2
- 同步带（2m）×1
- 同步轮（M5）×2
- 同步轮（M8）×1
- 固定轴承 ×1
- 联轴器 ×1

3. 连接件与电子配件
- L 型直角连接板 ×4
- 限位开关 ×2
- 定位固定件 ×2

4. 辅助配件与工具
- 型材端盖 ×4
- 工具包

## 视觉识别与补光

- 视觉链路（采集、标定、识别、JSON 导出）已迁移到同级独立仓库 `GhostVision`，相机硬件与取流参数见 `../GhostVision/docs/camera.md`
- 补光：`12V 2m 4000K` 正发光硅胶灯带
- 主仓库只接收标准化外部视觉结果，并将其投影为内部 `BoardState`

## 供电策略

- 前期优先使用各设备原装电源，降低联调复杂度
- 外设母线按 `12V` 设计，供步进电机、灯带、电磁铁使用
- 当前外设电源：`LRS-350-12`
- 控制板与 `12V` 外设母线必须共地

## 当前待继续验证的事项

- 若后续实测显示明显各向异性，再细化 `x+ / x- / y+ / y-` 四个方向的补偿值
- 在更高棋局密度下复验擦边经过和误吸情况
- 在实机上复验完整走子与吃子流程
- 在新备用棋盘与当前贴纸棋片形态下，继续复验 `x=370mm`、`y=337mm` 有效格区内的落点误差与误吸情况
